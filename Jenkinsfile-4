pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        sh '''
          curl --version
          git --version
          aws --version
        '''
      }
    }
  }
}
