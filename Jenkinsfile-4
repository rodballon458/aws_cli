pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        sh '''
          curl --version
          git --version
          aws --version
          jq --version
          psql --version
        '''
      }
    }
  }
}
